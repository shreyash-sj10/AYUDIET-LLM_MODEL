# QUICK REFERENCE: AYUDIET Strict LLM\n# Essential facts for developers\n\n## 🎯 What Changed?\n\nAYUDIET LLM is now **STRICT and SCHEMA-BOUND**:\n- X ❌ General-purpose chatbot\n- ✅ Controlled signal processor\n\n---\n\n## 📦 New Files to Import\n\n`python\nfrom llm_strict_wrapper import StrictLLMWrapper\nfrom llm_schemas_strict import AIProfileOutput_v1, StructuredFeedback_v1\nfrom llm_confidence_calibration import ConfidenceCalibrator\nfrom llm_hallucination_control import HallucinationDetector, OutputValidator\n`\n\n---\n\n## ⚡ Quick Start (Copy-Paste)\n\n### Initialize Wrapper\n`python\nfrom llm_strict_wrapper import StrictLLMWrapper\n\nllm = get_llm()  # Your existing LLM\nwrapper = StrictLLMWrapper(llm)\n`\n\n### Profile Extraction\n`python\nresult = wrapper.extract_health_profile(\n    user_input=\"I'm 30, weigh 70kg, have high blood pressure\",\n    current_profile={}\n)\n\nif result[\"success\"]:\n    profile = result[\"data\"]\n    confidence = result[\"confidence\"]\nelse:\n    profile = result[\"data\"]  # Still have fallback\n`\n\n### Feedback Parsing\n`python\nresult = wrapper.parse_feedback(\"I didn't like that recipe\")\n\nfeedback_type = result[\"data\"][\"feedback_type\"]  # LIKE/DISLIKE/REPLACE/UNCLEAR\ntarget = result[\"data\"][\"target\"]  # What it was about\n`\n\n### Explanation Generation\n`python\nresult = wrapper.generate_explanation(\n    context_chunks=[\"retrieved\", \"text\", \"chunks\"],\n    query=\"user question\"\n)\n\nexplanation = result[\"data\"][\"explanation\"]\nsources = result[\"data\"][\"sources\"]\nconfidence = result[\"data\"][\"confidence\"]\n`\n\n---\n\n## 🔒 Key Constraints\n\n### MANDATORY Rules\n\n| Rule | Implementation |\n|------|----------------|\n| JSON ONLY | Wrapper enforces |\n| NO markdown | Validators reject |\n| NO extra fields | Schema validation |\n| NO hallucinations | Detection system |\n| Confidence 0.0-1.0 | Calibration system |\n| Risk flags enum only | RiskFlag enum |\n| Dosha sum to ~1.0 | DoshaValidator |\n| Sources required (RAG) | Schema enforces |\n\n### Output Schemas\n\n**Profiling:**\n`json\n{\n  \"risk_flags\": [\"high_blood_pressure\"],\n  \"dosha_estimate\": {\"vata\": 0.3, \"pitta\": 0.5, \"kapha\": 0.2},\n  \"primary_dosha\": \"pitta\",\n  \"confidence\": 0.75\n}\n`\n\n**Feedback:**\n`json\n{\n  \"feedback_type\": \"DISLIKE\",\n  \"target\": \"moong_dal_recipe\"\n}\n`\n\n**Explanation:**\n`json\n{\n  \"explanation\": \"According to...\",\n  \"sources\": [\"Charaka Samhita\"],\n  \"confidence\": 0.8\n}\n`\n\n---\n\n## 📊 Confidence Thresholds\n\n`python\nfrom llm_confidence_calibration import ConfidenceThresholds\n\nif ConfidenceThresholds.should_process(confidence):\n    # confidence >= 0.45 - Process normally\n    use_output(result[\"data\"])\nelse:\n    # confidence < 0.45 - May want to ask for clarification\n    ask_user_for_more_info()\n\nif ConfidenceThresholds.should_use_fallback(confidence):\n    # confidence < 0.25 - Already using fallback\n    log_warning(f\"Low confidence: {confidence}\")\n`\n\n**Thresholds:**\n- HIGH: > 0.7\n- MEDIUM: 0.45-0.7\n- LOW: 0.25-0.45\n- VERY_LOW: < 0.25\n\n---\n\n## ✅ Testing Your Integration\n\n### Run Tests\n`bash\npython -m pytest test_llm_strict.py -v\n`\n\nExpected: All 40+ tests PASS ✅\n\n### Quick Unit Test\n`python\ndef test_my_agents():\n    from llm_strict_wrapper import StrictLLMWrapper\n    \n    llm = get_llm()\n    wrapper = StrictLLMWrapper(llm)\n    \n    # Test clear input\n    result = wrapper.extract_health_profile(\n        user_input=\"I'm 28, diabetic, high blood pressure\"\n    )\n    assert result[\"success\"]\n    assert result[\"confidence\"] > 0.6\n    \n    # Test vague input\n    result = wrapper.extract_health_profile(\n        user_input=\"I feel bad\"\n    )\n    assert result[\"confidence\"] <= 0.5\n    assert result[\"data\"] is not None  # Has fallback\n`\n\n---\n\n## 🚫 Common Mistakes (AVOID!)\n\n### ❌ Mistake 1: Ignoring Fallback\n`python\n# WRONG\nresult = wrapper.extract_health_profile(...)\nif result[\"success\"]:\n    use(result[\"data\"])\nelse:\n    return None  # Lost fallback!\n\n# CORRECT\nresult = wrapper.extract_health_profile(...)\nuse(result[\"data\"])  # Use fallback if error\nif result[\"success\"]:\n    # Confidence is real, log it\n    log_confidence(result[\"confidence\"])\n`\n\n### ❌ Mistake 2: Bypassing Wrapper\n`python\n# WRONG\nresponse = llm.invoke(...)  # Direct call\njson_data = json.loads(response.content)  # No validation\nuse(json_data)\n\n# CORRECT\nresult = wrapper.extract_health_profile(...)  # Use wrapper\njson_data = result[\"data\"]  # Already validated\nuse(json_data)\n`\n\n### ❌ Mistake 3: Ignoring Confidence\n`python\n# WRONG\nresult = wrapper.extract_health_profile(...)\nuse(result[\"data\"])  # No checking of confidence\n\n# CORRECT\nresult = wrapper.extract_health_profile(...)\nif result[\"confidence\"] < 0.5:\n    log.warn(f\"Low confidence assessment: {result['confidence']}\")\n    maybe_ask_for_clarification()\nuse(result[\"data\"])\n`\n\n---\n\n## 🔍 Debugging\n\n### Debug Confidence Score\n`python\nfrom llm_confidence_calibration import ConfidenceCalibrator\n\nuser_input = \"I feel off sometimes\"\nconfidence = ConfidenceCalibrator.get_confidence_score(user_input)\nambiguity = ConfidenceCalibrator.detect_ambiguity(user_input)\n\nprint(f\"Confidence: {confidence:.2f}\")\nprint(f\"Vague terms: {ambiguity['vague_terms_found']}\")\nprint(f\"Too short: {ambiguity['too_short']}\")\n`\n\n### Debug Validation Error\n`python\nfrom llm_hallucination_control import OutputValidator\n\nis_valid, obj, error = OutputValidator.validate_profile_output(raw_data)\nif not is_valid:\n    print(f\"❌ Validation Error: {error}\")\n    print(f\"Data was: {raw_data}\")\nelse:\n    print(f\"✅ Valid! Risk flags: {obj.risk_flags}\")\n`\n\n### Debug Hallucination Detection\n`python\nfrom llm_hallucination_control import HallucinationDetector\n\nexplanation = \"This cure cures diabetes forever with 100% guarantee\"\nif HallucinationDetector.has_ungrounded_claims(explanation):\n    print(\"⚠️  Ungrounded medical claims detected\")\n    confidence *= 0.5  # Reduce confidence\n`\n\n---\n\n## 📋 Production Checklist\n\nBefore deploying:\n\n- [ ] Run test suite: `pytest test_llm_strict.py -v`\n- [ ] All tests pass ✅\n- [ ] Updated 3+ agents to use StrictLLMWrapper\n- [ ] Tested with real user data\n- [ ] Confidence scores logged\n- [ ] Fallback paths tested\n- [ ] Documentation reviewed by team\n\n---\n\n## 📚 Documentation Map\n\n| Document | Purpose | When to Read |\n|----------|---------|---------------|\n| `AYUDIET_LLM_STRICT_SPEC.md` | Complete specification | Before integrating |\n| `AYUDIET_STRICT_LLM_INTEGRATION.md` | Integration examples | While integrating |\n| `DELIVERY_SUMMARY.md` | What was delivered | Understanding overall changes |\n| `test_llm_strict.py` | Test examples | Testing your code |\n| **THIS FILE** | Quick reference | While coding |\n\n---\n\n## ⚡ TL;DR\n\n1. **Import:** `from llm_strict_wrapper import StrictLLMWrapper`\n2. **Initialize:** `wrapper = StrictLLMWrapper(llm)`\n3. **Use:** `result = wrapper.extract_health_profile(...)`\n4. **Get data:** `profile = result[\"data\"]` (always valid)\n5. **Check confidence:** `if result[\"confidence\"] < 0.5: warn()`\n6. **Test:** `pytest test_llm_strict.py -v`\n7. **Deploy:** Replace LLM calls in agents\n\n---\n\n## 🎯 Your LLM is Now\n\n✅ STRICT (schema-bound) \n✅ RELIABLE (hallucinations detected) \n✅ CONTROLLABLE (confidence calibrated) \n✅ SAFE (fallbacks guaranteed) \n✅ TESTABLE (40+ test cases) \n✅ PRODUCTION-READY \n\nDeployment time! 🚀\n
